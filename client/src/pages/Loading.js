import React from 'react';
import "../styles/Loading.css"
const Loading = () => {
    return (
        <div className="loading">
            LOADING...
        </div>
    );
}

export default Loading;
